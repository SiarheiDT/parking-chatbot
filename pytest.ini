[pytest]
testpaths = tests
python_files = test_*.py
markers =
    stage1: user chatbot, RAG, DB, guardrails (Stage 1)
    stage2: Telegram admin webhook, notifications, LangChain admin tools (Stage 2)